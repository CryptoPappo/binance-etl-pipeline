# Binance Trading Data ETL Pipeline
This project implements an end-to-end ETL pipeline that collects a trading pair 
aggregate trades data from the Binance API, transforms it into clean analytical tables, 
and stores it into a PostgreSQL database for downstream analytics.
